from setuptools import setup

setup(
    name='downloader',
    version='1.0.0',
    packages=['downloader'],
    url='',
    license='',
    author='baba',
    author_email='',
    description='downloader',
    requires=["pyNfsClient", "tqdm", "click"]
)
